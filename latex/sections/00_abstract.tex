% O GIQ, como o resto da Elsevier, quer o abstract em BLOCO UNICO, sem quebra de
% paragrafo. As transicoes abaixo foram escritas para o texto correr como um
% paragrafo so.
%
% A ABERTURA E DELIBERADA. Duas lentes independentes da auditoria externa
% (journal-specialist e didatica) leram a versao anterior — que abria pelo
% protocolo e pelas estatisticas — como escrita para um publico de ML, com risco
% de desk rejection numa revista de administracao publica. Esta versao abre pela
% pergunta que o gestor faz, e so entao diz como medimos. Os numeros sao os
% mesmos; o que mudou foi a ordem de entrada.
\begin{abstract}

A municipal environmental manager in S\~ao Paulo, Lagos, or Jakarta asks a
language model what the binding PM\textsubscript{2.5} standard is in their
country. Nothing in the answer says whether it matches the official register, and
no established method exists for checking. We contribute such a protocol and
report what it finds. It scores answers against official registers, adjudicating
by code wherever the correct answer is a published value and by a three-vendor
judge panel elsewhere. We apply it to air pollution regulation across 25
countries, 14 deployment stacks and four languages (8{,}244 scored cells). The three
fixes an agency can deploy without building anything all fail, and the most
intuitive one makes the answer worse: querying in the country's own language
\emph{lowers} accuracy by 5.0 percentage points (Wilcoxon $p=5\times10^{-16}$) and makes the model 9.4
points less likely to return a register-checkable value at all; declaring the
official role does not narrow the gap ($p=0.29$); and a regionally tuned model
ranks last of fourteen. Accuracy also collapses on the task that carries
administrative consequence, retrieving the binding value: 0.39 against 0.61 for
synthesis and recommendation. A North/South gap persists ($+5.0$~pp, permutation
$p=0.021$), concentrated where answers depend on a national fact --- on the
national standard, scored by code against the instrument in force, Global South
odds are 0.33 of Global North odds. Scope is
bounded: we measure agreement with published regulation in one domain, not general
capability. The protocol transfers to any regulated domain whose correct answer is
published.

\end{abstract}
